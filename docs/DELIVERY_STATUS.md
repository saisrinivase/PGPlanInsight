# PGPlan Insight delivery status

## Existing-index benchmark expansion — 2026-09-25

- Expanded synthetic scoring from 5 to 11 decisions with an existing-index cohort: material and harmless repetition, residual filters, missing heap capture, and read-counter interpretation. Exact diagnostic signals and explanation fragments are separately gated.
- Accuracy command, all 134 unit tests and static security passed. No application behavior/UI changes. Independent DBA review, holdout corpus and full root-cause accuracy remain unestablished. Hosted Windows/Linux validation was still running during inspection; no production-ready claim.

## Diagnostic scoring harness — 2026-09-25

- Added `npm run test:accuracy`: five explicitly synthetic plan-only finding decisions, per-family confusion counts, precision/recall, false positives, abstention and decision coverage. Unknown denominators stay null; any expected-decision mismatch fails CI. Reports are archived separately for Windows/Linux before browser tests can clear test-results.
- Passed 134 unit tests, including scoring failure/invalid-input tests, static security and diff checks. No runtime UI or diagnostic rule changed; browser testing is delegated to the existing hosted release gate.
- PARTIAL benchmark coverage: independent DBA labels, holdout evaluation, cast/spill/estimate/index-coverage cases, context-assisted cohorts and recommendation-safety scoring remain outstanding. No production accuracy percentage claimed.

## Windows release validation — 2026-09-22

- Replaced direct npm process spawning with the npm JavaScript entry invoked through `process.execPath`, without a shell. Added Windows paths-with-spaces, POSIX audit and missing-entry tests.
- Added Windows alongside Ubuntu to the full release CI matrix; failure artifacts are OS-specific. Existing test coverage and performance thresholds are unchanged.
- Local verification: 132 unit tests, static security and diff checks passed. Windows release acceptance remains PARTIAL until the hosted Windows gate passes; local macOS tests are not proof of Windows behavior.

## Index recommendation sample SQL — current incremental slice

- Implemented a Findings decision explaining whether to investigate existing coverage or test a candidate. Physical sample SQL requires a schema-qualified relation and qualified imported catalog evidence; duplicate/overlap/incomplete states withhold it. No database execution or promised speedup.
- Validation: 129 unit tests, production build, static security scan, and 15 focused browser checks across all five profiles passed. `git diff --check` passed. Restored dependencies with locked `npm ci --ignore-scripts --prefer-offline` after duplicate type-library folders caused the first build to fail.
- PARTIAL release acceptance: full release gate and manual visual inspection not rerun for this slice. No push performed. Deeper operator-class/collation matching and DBA-labelled runtime validation remain pending; this is not completion of the broader access-path roadmap.

Updated: 2026-09-13

| Stage | Status | Implementation evidence | Test evidence | Security evidence | Remaining risk |
|---|---|---|---|---|---|
| 1. Evidence classifications | COMPLETE | Canonical typed `observed`, `derived`, `suspected`, `unknown`, and `verified` claims drive Findings and the DBA report export | 51 unit tests; 90 active full-suite browser tests across five projects; 10 focused post-hardening browser tests | No application HTML injection sink remains; no browser secrets detected; outbound regression confirms analysis remains same-origin | Production bundle retains a pre-existing >500 kB visualization warning; verified findings require Fix Validation by design |
| 2. Database Context Pack | COMPLETE | Versioned v2 schema, v1 normalization, provenance/completeness, sanitized preview, 1 MB and collection limits, safe-setting allowlist, read-only collector SQL, privacy documentation, and existing-index qualification are implemented; the shipped collector executed successfully on PostgreSQL 18.3 | 56 unit tests and 95 active five-project browser workflows pass; PostgreSQL 14/16/18 parser fixtures pass; live PostgreSQL 18.3 output passed the application validator with zero discarded fields or warnings; 10 focused cross-browser workflows passed after live validation | Sensitive/unknown fields are removed before application; unredacted packs and unsafe settings are rejected; no browser secrets or unsafe HTML sinks; only same-origin sample fetches remain; the live pack was written to `/tmp` and inspected using counts and a SHA-256 digest without printing catalog names | Runtime execution was available for PostgreSQL 18.3 only; PostgreSQL 14 and 16 compatibility is covered by deterministic parser fixtures and catalog-surface tests rather than live servers |
| 3. Statistics/cast/index-only diagnosis | COMPLETE | Typed rules distinguish the first row-producing estimate divergence, plan-only unknowns, modification-pressure and extended-statistics hypotheses, existing extended statistics, predicate/output/set-operation/materialized-view coercion, estimated-only scans, heap-fetch-heavy access, residual filters, measured loop amplification, effective zero-heap access, and missing evidence | 74/74 unit tests pass across 15 files; positive, negative, incomplete, TEXT/JSON, expression/UNION, extended-statistics, heap-fetch, residual-filter, loop-amplification, and legacy-history fixtures pass; production build passes; full browser suite passes 95 active workflows across Chromium, Firefox, WebKit, mobile Chromium, and mobile WebKit with 45 intentionally skipped retired-renderer tests | Offline production dependency audit reports 0 vulnerabilities; no unsafe HTML/eval sink, browser secret, new dependency, or unexpected outbound request; inputs remain bounded and React renders evidence as text | PostgreSQL 14/16/18 compatibility uses deterministic fixtures while live collector validation is PostgreSQL 18.3; older saved analyses cannot recover previously discarded Heap Fetches; >500 kB visualization bundle warning remains |
| 4. Candidate-index experiments | COMPLETE | Typed controlled experiments expose predicate evidence, equality-before-range rationale, context-aware duplicate/overlap qualification, write/storage risks, confidence, optional HypoPG planning checks, success gates, and rollback; plan-only primary output is a non-executable shape; dense Plan, Grid, and Raw modes expose complete content without fixed-height clipping | 85/85 current unit tests pass; production build passes; 15 focused dense-renderer checks and 115 active full-suite browser workflows pass across five projects, with 45 intentionally skipped retired-renderer tests | Offline production dependency audit reports 0 vulnerabilities; no unsafe sink, browser secret, new dependency, execution path, or unexpected outbound request; React renders evidence as text | HypoPG proves planner choice only, never runtime; imported context cannot establish workload recurrence or write impact; the existing >500 kB visualization bundle warning remains |
| 5. Environment drift | COMPLETE | Browser-local PTEST-reference versus production-target comparison covers PostgreSQL version, relations, column types, index shape/state, column and relation statistics, extended statistics, partitions, and safe planner settings; unavailable sections are explicitly not comparable; bigint/double-precision drift is explained without claiming causality | 85/85 unit tests pass including exact-match, false-positive, missing-object, invalid-index, bigint/double, and incomplete-evidence cases; production build passes; focused workflow passes 5/5 browser projects; complete suite passes 115 active workflows across five projects with 45 retired tests skipped | Both packs pass the existing redacted 1 MB boundary; no credentials, SQL text, host/database identity, statistic values, unsafe HTML, browser secret, new dependency, or external transmission; offline production audit reports 0 vulnerabilities | Metadata drift qualifies a hypothesis but cannot prove workload recurrence or runtime causality; comparable repeated before/after plans remain required |
| 6. Security release gate | COMPLETE | `npm run release:gate` now cleans `dist`, performs production build/type checks, unit and five-project browser QA, static secret/sink/outbound checks, offline advisory audit, license policy, CycloneDX SBOM, atomically writes the machine-readable result, builds the SHA-256 manifest, and verifies the complete release contract | Accepted unified gate: locked install passed; 87/87 unit tests; 135 active browser workflows passed with 45 retired tests skipped; production build passed; all 30 manifest entries independently hash-validated; isolated negative checks rejected both corrupt and empty release reports | No known exploitable high/critical production dependency finding reported by the configured offline gate; 46 source files passed static checks; CSP/referrer and no-outbound behavior verified; 101 production components/licenses inventoried; production index and bundled font assets verified | Current network advisory data and HTTP-only edge headers must be verified in the approved deployment environment; meta CSP cannot enforce clickjacking or `nosniff`; >500 kB visualization bundle warning remains |
| 7. Governed AI Review | REMOVED | Separate AI workspace, packet builder, route, styling, and tests removed by product decision | Active navigation regression verifies the tab is absent | No provider integration, browser API key, or external AI transmission path exists | AI-assisted explanation may be reconsidered later only as a separately governed enterprise integration |
| 8. Workload triage | COMPLETE | A pre-analysis workspace imports a sanitized `pg_stat_statements` snapshot, ranks fingerprints by captured total execution time, distinguishes counter evidence from unknown root cause, identifies the strongest review direction, and starts a labeled plan investigation | 104 unit tests and 195 active five-profile browser workflows pass; live PostgreSQL 18.3 collector output containing 1,056 aggregated fingerprints passed the product parser; desktop and narrow layouts were visually inspected | Read-only collector excludes SQL text, users, databases, hosts, parameters, credentials, and object names; strict 2 MB/5,000-row schema boundary; unsupported fields and SQL text are rejected; no outbound request occurs | Ranking reflects cumulative counters since reset, can combine parameter values, and cannot establish p95 latency, concurrency, lock waits, cache state, plan cause, or fix effectiveness |

Allowed status values: `NOT STARTED`, `IN PROGRESS`, `PARTIAL`, `BLOCKED`, `COMPLETE`, `REMOVED`.

## Active slice

- Workspace: Validate fix quality pass (COMPLETE)
- User-visible outcome: The workspace now starts with the captured before-plan baseline, separates comparability declarations from measured evidence, and preserves blocker/warning/pass results before allowing an improvement claim.
- Evidence boundary: Same SQL, representative parameters, comparable environment, and repetition are operator declarations and are explicitly labeled as not verified from the plan. Missing runtime or captured setting drift blocks the verdict.
- Presentation: All analysis workspaces use bundled IBM Plex Sans, with IBM Plex Mono reserved for evidence values. Navigation is compact, numbered, horizontally safe on narrow screens, and uses a restrained teal active indicator; the approved Plan geometry is unchanged.
- QA evidence: 112/112 unit tests; 20/20 focused cross-browser/mobile tests; and the complete release gate passed with 200 active browser tests across five profiles, 45 retired-renderer tests skipped, plus 100–2,000-node performance and repeated-navigation stress coverage.
- Security evidence: Static checks passed 50 source files; dependency install reported zero vulnerabilities; CSP, referrer, same-origin behavior, SBOM, licenses, and all 24 artifact hashes passed.
- Known limitations: Manual declarations cannot prove cache state, concurrency, parameter representativeness, or workload-wide benefit. Production approval still requires representative repeated execution.
- Exact next workspace: Database context quality pass, preserving the approved Plan, Findings, Planner diagnostics, and Validate fix workspaces.


## Security and privacy hardening — 2026-09-10

Implementation complete for the reviewed code risks. The previous offline-gate claims above are historical and superseded by this verification.

- Temporary analysis by default; explicit 1/7/30-day retention; per-case deletion; 50-case cap; legacy expiry and revalidation on reopen.
- Identifier/expression redaction preview with explicit loss-of-evidence notice; clearer file failures and capture guidance; narrow-screen textarea width fixed.
- Byte/depth/node/field/type checks before analysis and rendering; isolated workers with timeout/error recovery; context nesting bounded.
- Vitest upgraded to patched 4.1.11. The gate performs npm ci, tests, production build, production-preview browser tests, static checks and online audits of both dependency scopes. Evidence is fresh, source/lock-hash bound and required for artifact generation.
- Passed: 92 unit tests; 155 browser workflows across Chromium/Firefox/WebKit plus mobile Chromium/WebKit; 45 intentionally skipped retired-renderer workflows; build; 47-file static check; zero known advisories from both online audits; license/SBOM/hash verification.
- Negative checks: missing gate evidence prevents artifact generation; invalid report fails verification.
- Visual checks: desktop intake, 390px mobile intake and redaction preview inspected in the browser. Existing dense-plan, Findings, context and validation workflows passed cross-browser tests.
- Header protections tested in the production preview; deploy/nginx.conf documents production configuration. Actual deployment headers remain to be verified at the selected host.
- Limits: bundle-size warning remains; independent DBA-labeled real-world accuracy benchmarking and repeated-run statistical comparison are future product work (docs/PRODUCT_FEEDBACK.md). No production database was accessed, and no diagnostic superiority is claimed. Project licensing remains an owner decision.

## Release acceptance and scale validation — 2026-09-11

- Added production-browser acceptance coverage for complete Plan, Grid, and Raw rendering at 100, 500, 1,000, and the supported 2,000-node ceiling.
- Added optimized large clipboard handling that preserves the selected replacement range, enforces the existing 10 MB boundary, and remains editable after rejection.
- Added a 20-cycle analysis/navigation stress workflow with page-error detection and a per-cycle 15-second guardrail.
- The unified release gate passed from a clean locked install: 92/92 unit tests; 185 active browser workflows across Chromium, Firefox, WebKit, mobile Chromium, and mobile WebKit; 45 retired-renderer workflows intentionally skipped; production build; static security checks; dependency audits; SBOM; license inventory; and artifact verification.
- Maximum measured 2,000-node analyze-to-Grid time was 11.9 seconds in desktop Firefox and 8.5 seconds in mobile WebKit; all profiles remained below the 30-second acceptance ceiling. These are local release-gate measurements, not production service-level guarantees.
- The generated release contains one canonical production artifact set. All 31 SHA-256 manifest entries independently validated, `release-report.json` is readable and reports `PASS`, the production index references hashed bundles, and no suffixed duplicate artifacts exist.
- No database or network service is involved in the stress test; browser-level Playwright coverage is the relevant load model. The known Vite warning for visualization and host bundles above 500 kB remains non-blocking and should be addressed as a future loading-performance improvement.

## Dense DBA Workbench theme — 2026-09-14

- Applied bundled IBM Plex Sans and a dense navy/gray visual system across Plan, Findings, Planner diagnostics, Validate fix, and Database context without changing diagnostic behavior.
- Corrected the final theme override so analysis workspaces use the complete canvas without the earlier outer side gutters.
- Removed excessive desktop gutters and improved compact/mobile readability; the Plan renderer uses the same typeface inside its shadow DOM.
- Passed 112 unit tests, production build, 200 active cross-browser workflows, stress/2,000-node coverage, static security checks, and release-artifact verification.
- Automated fixture conformance is complete. Real-world diagnostic accuracy is not represented as a percentage until an independent DBA-labelled benchmark exists; the measurement contract is documented in `docs/DIAGNOSTIC_ACCURACY.md`.
- Findings and Planner diagnostics now use the dense workbench rhythm: compact headings, 32–40 px evidence rows, reduced disclosure padding, full-width canvas use, and IBM Plex Sans/Mono hierarchy. Diagnostic ordering, evidence boundaries, and controls are unchanged.
- Index-only diagnosis now reports a measured evidence profile (time share, loops, returned row visits, shared reads, heap fetches, and residual filtering), selects the highest-time qualifying scan, and suppresses the duplicate generic loop finding when both describe the same operation. Estimated cost alone still cannot trigger the diagnosis.
- Accuracy regression evidence: 114 unit tests and 15 focused Findings/Planner workflows across Chromium, Firefox, WebKit, mobile Chromium, and mobile WebKit passed; the production build passed. Root cause remains a bounded plan-visible diagnosis until catalog and representative workload evidence are supplied.

## Predicate/index accuracy — first bounded slice, 2026-09-14

- Plain-column candidates now require a fully supported conjunction of simple comparisons. Functions, column casts, OR, unsupported operators, and mixed unsupported expressions cause abstention rather than partial regex extraction. SQL string contents cannot introduce fake keys.
- Existing-prefix qualification requires explicitly valid, ready, nonpartial, nonexpression B-tree metadata. Partial/expression indexes, INCLUDE overlap, and unknown metadata require review. Unavailable index inventory cannot establish index absence. This is structural matching, not proof of operator-class compatibility or planner selection.
- 128 unit tests, 15 focused cross-browser Findings/Planner workflows, production build, and static security gate passed. Regression cases are developer-labelled, not independent DBA validation. The large-bundle build warning remains; the full release gate was not rerun for this slice.
- Remaining roadmap: SQL/view/set-operation lineage, cast-impact qualification, richer coverage/overlap reasoning, statistics qualification, and independent DBA-labelled positive/negative benchmarks. No competitive accuracy percentage is claimed.

## Classic DBA Report — 2026-09-15

- User selected preview #2, superseding IBM Plex: Verdana interface, 14px body text, compact ruled tables, white workspace, pale report headers, square controls, and 12px outer workspace padding. SQL/raw text retains monospace. Shared presentation applied across workspaces; Plan shadow-root typography updated without changing renderer behavior or diagnostic logic.
- Corrected disclosure-label overlap and wrapping. Final focused run: 35/35 workflows across Chromium, Firefox, WebKit and both mobile projects passed, including font consistency and document overflow checks. Initial run had three load timeouts and one mobile overflow; final run passed after layout fixes. 128 unit tests, production build and static security checks passed. Full release gate not run; existing bundle-size warning remains.
- Desktop screenshots inspected for Findings, Planner diagnostics and Validate fix. User visual acceptance remains pending; no diagnostic accuracy claim follows from styling checks.

## Release-check repair — 2026-09-16

- GitHub run 34970273996 failed nine browser cases: five copies of an obsolete index-coverage expectation, three 2,000-node runtime limits, and one clipboard renderer wait. Dependency install and browser installation passed. Local targeted checks previously reported did not cover all release cases.
- Corrected the positive index fixture to explicitly capture ready/valid B-tree, nonpartial and nonexpression metadata; missing metadata still requires review. CI browser workers are serialized to avoid benchmark CPU contention; the 30-second rendering budget remains unchanged. Clipboard readiness now uses the same 30-second allowance as large-file intake. Failure traces are retained for seven days.
- Full local release gate passed: 128 unit tests, 200 active browser tests (45 retired-renderer tests skipped), production build, static security, dependency audits and release artifacts (32 hashes). Hosted result remains pending; do not infer a passing release from a successful push. Type-lineage implementation remains next after the hosted gate is green.

## Production readiness — benchmark instrumentation, 2026-09-20

- Hosted run 35219669851 passed 199 browser cases; Firefox's 2,000-node benchmark recorded 32,089ms against its unchanged 30,000ms budget.
- Controlled local comparison, two runs each on the same build: full tracing 11,161/11,195ms; lightweight action tracing 6,455/6,510ms. Repeated DOM snapshots materially inflate this benchmark. Performance tests now omit DOM snapshots and trace screenshots, retain action logs and failure screenshots, and attach timing evidence before asserting the budget. Functional tests retain full traces. This is measurement correction, not a claimed application speedup.
- Full local release gate passed: 128 unit tests, 200 active browser tests (45 retired-renderer tests skipped), build, static checks, dependency audits, and release-artifact validation (32 hashes). Remaining production requirements: hosted green gate, independently DBA-labelled diagnostic benchmark, context/type lineage, pilot-user acceptance, versioned release and rollback documentation. No production-ready claim until these gates are satisfied.
